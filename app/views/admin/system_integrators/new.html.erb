<!-- Content Header (Page header) -->
<div class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
        <h1 class="m-0"><%= @system_integrator.new_record? ? 'Create System Integrator' : 'Edit System Integrator' %></h1>
      </div>
      <div class="col-sm-6">
        <ol class="breadcrumb float-sm-right">
          <li class="breadcrumb-item"><a href="<%= admin_dashboard_path %>">Dashboard</a></li>
          <li class="breadcrumb-item"><a href="<%= admin_system_integrators_path %>">System Integrators</a></li>
          <li class="breadcrumb-item active"><%= @system_integrator.new_record? ? 'New' : 'Edit' %></li>
        </ol>
      </div>
    </div>
  </div>
</div>

<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card card-primary">
          <div class="card-header p-0 pt-1">
            <!-- Add any additional header content if needed -->
          </div>
          <%= render 'form', system_integrator: @system_integrator %>
        </div>
      </div>
    </div>
  </div>
</section>
