<% if object.errors.any? %>
  <h4 class="alert-heading"><%= pluralize(object.errors.count, "error") %> prohibited this enquiry from being saved:</h4>
  <ul>
    <% object.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
